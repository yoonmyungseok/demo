- route:
    id: rest-test
    from:
      uri: rest:post:rest-test
      steps:
        - set-body:
            constant: >
              {"id":"yoonms5319"}
        - unmarshal:
            json: { }
        - log: ${headers}
        - log: ${body}
#        - to: rest:post:/http-test?host=localhost:1235&bridgeEndpoint=true